name: build

on:
  push:
    branches: [ main ]
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Web en reconstrucción
        run: |
          echo "La web se reinició el 2026-07-22: content/ y build.py se rehacen desde cero."
          echo "Este workflow se recablea (build + sanity checks + artefacto dist/) al reconstruir el generador."
